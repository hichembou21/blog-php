<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8"/>
        <title>Blogs</title>
        <link href='//fonts.googleapis.com/css?family=Lato:300' rel='stylesheet' type='text/css'>
        <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css" integrity="sha384-Gn5384xqQ1aoWXA+058RXPxPg6fy4IWvTNh0E263XmFcJlSAwiGgFAW/dAiS6JXm" crossorigin="anonymous">
        <link rel="stylesheet" href="/css/style.css">
    </head>
    <body>
        <header>
        <nav class="navbar navbar-dark bg-primary">

            <a class="navbar-brand" href="/"><img src="/img/blog.png" alt="blog picture" width="100" height="70"></a>
            <form class="form-inline" action="{{path_for('login')}}" method="POST" id="form-sign-in">

                <div class="form-group col-md-4">
                    <label>Email address</label>
                    <input type="email" class="form-control" name="email" placeholder="your email">
                </div>
                <div class="form-group col-md-4">
                    <label>Password</label>
                    <input type="password" class="form-control" name="password" placeholder="Password">
                </div>
                <a href="{{path_for('inscription')}}">Create a count!</a>
                            
                <button type="submit" class="btn btn-dark">Sign in</button>
        
            </form> 
        </nav>
        </header>  
        
        <section class="corp"> 
            <h1 class="title text-center">{{user.blog}} </h1>        
            
                {% for article in articles %}
                <div class="row featurette presentation-blog">
                    <div class="col-md-7">
                      <h3 class="featurette-heading"><span class="text-muted"> {{article.title}}</span></h3>
                      <p class="lead">{{article.content}}</p>
                    </div>
                    <div class="col-md-5">
                      <img class="img-desc featurette-image img-fluid mx-auto" alt="500x400" style="width: 500px; height: 400px;" src="{{article.picture}}" data-holder-rendered="true">
                    </div>
                </div>
                {% endfor %}
            
        </section>
    

    <footer>
        <div class="card-footer text-muted">
            fin de page
        </div>
    </footer> 
    
    <script src="/script/app.js" type="text/javascript"></script>
    
    </body>
</html>
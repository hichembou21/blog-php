<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8"/>
        <title>App Blogs</title>
        <link href='//fonts.googleapis.com/css?family=Lato:300' rel='stylesheet' type='text/css'>
        <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css" integrity="sha384-Gn5384xqQ1aoWXA+058RXPxPg6fy4IWvTNh0E263XmFcJlSAwiGgFAW/dAiS6JXm" crossorigin="anonymous">
        <link rel="stylesheet" href="/css/style.css">
    </head>
    <body>
        <header>
        <nav class="navbar navbar-dark bg-primary">

            <a class="navbar-brand" href="/"><img src="/img/blog.png" alt="blog picture" width="100" height="70"></a>
            <form class="form-inline" action="{{path_for('login')}}" method="POST" id="form-sign-in">

                <div class="form-group col-md-4">
                    <label>Email address</label>
                    <input type="email" class="form-control" name="email" placeholder="your email">
                </div>
                <div class="form-group col-md-4">
                    <label>Password</label>
                    <input type="password" class="form-control" name="password" placeholder="Password">
                </div>
                <a href="{{path_for('inscription')}}">Create a count!</a>
                            
                <button type="submit" class="btn btn-dark">Sign in</button>
        
            </form> 
        </nav>
        </header>
        
        {% if (error) %}
            <h4> {{ error }} </h4> 
        {% endif %}
        
        <section class="corp"> 
            <h1 class="title text-center">All Blogs </h1>        
            <table class="table">
            <thead class="thead-dark">
                <tr>
                <th scope="col">#</th>
                <th scope="col">Username</th>
                <th scope="col">Blog name</th>                
                <th scope="col">Operation</th>                 
                               
                </tr>
            </thead>
            <tbody>
                {% for user in users %}
                <tr>
                <th scope="row">{{user.id}}</th>
                    <td> {{user.username}} </td>
                    <td> {{user.blog}} </td>
                    <td> <a href="{{ path_for('editblog', {'id':user.id}) }}">Edit</a> </td>
                    
                </tr>
                {% endfor %}
            </tbody>
            </table>
        </section>
    

    <footer>
        <div class="card-footer text-muted">
            fin de page
        </div>
    </footer> 
    
    <script src="/script/app.js" type="text/javascript"></script>
    
    </body>
</html>
